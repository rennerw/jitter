//import { ItemRepository } from '../repositories/item.repository';

// Aqui optei por deixar em portugues por se tratar de algo relacionado 
// a regras de negócio e menos padronizado que os metodos de controller
export class ItemService {
  /*private readonly repository: ItemRepository;
 
  constructor(repository: ItemRepository) {
    this.repository = repository;
  }
 
  async criar(input: CreateItemDto) {
    // lógica de validação ou regras de negócio
    return this.repository.criar(input);
  }
 
  async listarTodos() {
    // pode aplicar filtros ou paginação
    return this.repository.listarTodos();
  }
 
  async encontrarPorId(id: number) {
    // pode lançar erro se não encontrar
    return this.repository.encontrarPorId(id);
  }
 
  async atualizar(id: number, input: UpdateItemDto) {
    // lógica de merge ou validação
    return this.repository.atualizar(id, input);
  }
 
  async remover(id: number) {
    // pode verificar dependências antes de remover
    return this.repository.remover(id);
  }*/
}

